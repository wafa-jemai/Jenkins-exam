replicaCount: 1

movie:
  image:
    tag: movie.qa

cast:
  image:
    tag: cast.qa

service:
  type: NodePort
  port: 80
  targetPort: 8000
  nodePort: 30082

env:
  DATABASE_URL: "sqlite:///./app/api/db.sqlite3"

serviceAccount:
  create: true
  name: ""

autoscaling:
  enabled: false

ingress:
  enabled: false

