apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkins-exam.fullname" . }}-test-connection"
  labels:
    {{- include "jenkins-exam.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jenkins-exam.fullname" . }}:{{ .Values.service.port }}/api/v1/checkapi']
  restartPolicy: Never
