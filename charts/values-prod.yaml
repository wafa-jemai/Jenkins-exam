replicaCount: 3

movie:
  image:
    repository: wafajemai/jenkins-devops
    tag: "movie.prod"

cast:
  image:
    repository: wafajemai/jenkins-devops
    tag: "cast.prod"

service:
  type: NodePort
  port: 80
  nodePort: 30084
  targetPort: 8000

env:
  DATABASE_URL: "sqlite:////data/prod.db"
