replicaCount: 1

movie:
  image:
    repository: wafajemai/jenkins-devops
    tag: "movie.qa"

cast:
  image:
    repository: wafajemai/jenkins-devops
    tag: "cast.qa"

service:
  type: NodePort
  port: 80
  nodePort: 30082
  targetPort: 8000

env:
  DATABASE_URL: "sqlite:///./app/api/db.sqlite3"

